function filterModel(description) {
    var desc = description.toString
    var splits = desc.split("<br/>")
//    var splits = "I<br/>am<br/>your<br/>father".split("<br/>")
    var raw = "null"
    console.log(desc)
//    raw = splits
    return splits
}
